import "../App.css";
const Productlist = ({ product }) => {
  return <div>
  {product.map((productItem, productIndex) => {

    return(
        <div>

        </div>
    )
  })}
  </div>;
};
export default Productlist;
